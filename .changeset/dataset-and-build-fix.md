---
"@_linked/dcat": minor
---

- `feat`: add `dcat:Dataset` class and `dcat:servesDataset` property to the ontology
- `fix(build)`: switch to explicit per-step pipeline so silent build failures no longer ship empty tarballs
